# Enforce pnpm as the only allowed package manager.
# Running `npm install` or `yarn install` will fail with a clear message
# thanks to the "preinstall": "npx only-allow pnpm" script in package.json.

engine-strict=true
shamefully-hoist=true
